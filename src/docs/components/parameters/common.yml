PageQuery:
  name: page
  in: query
  required: false
  description: Номер страницы
  schema:
    type: integer
    default: 1
    minimum: 1
    example: 1

LimitQuery:
  name: limit
  in: query
  required: false
  description: Количество элементов на странице
  schema:
    type: integer
    default: 10
    minimum: 1
    maximum: 100
    example: 10

RoleId:
  name: id
  in: path
  required: true
  description: ID роли
  schema:
    type: integer
    example: 1